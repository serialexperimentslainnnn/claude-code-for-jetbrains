# Map of `resources/jcef/`

> Part of the distributed map. **Root: [`../../../../PROJECTMAP.md`](../../../../PROJECTMAP.md)**
> — repository-wide commands, invariants and the index of every other directory map live there.

## What lives here

**The chat UI.** An inlined web app under a strict, hash-pinned CSP: no CDN, no external resource, nothing
fetched at runtime. It is the whole visible surface of a conversation — transcript, composer, cards,
dashboard and tab bar.

**There is no module system in the page.** Each file is its own hash-pinned `<script>`, and they meet through
`window.cc` / `window.CC`. That single fact produces most of the rules below.

The host half is [`../../kotlin/dev/lain/claudejb/ui/jcef/`](../../kotlin/dev/lain/claudejb/ui/jcef/PROJECTMAP.md):
it decides every payload and every state word, and the page paints what it is sent.

## The families

| Family | Files | What it owns |
|---|---|---|
| **core** | `app-core.js`, `-markdown`, `-diagram`, `-theme` | The bridge helpers, the null-safe `cc.*` registry, rendering and sanitising model text, the diagram, the theme variables. |
| **transcript** | `app-transcript.js` + `-rows`, `-tools`, `-links`, `-find` | The rows, the tool cards, the links, the find bar. |
| **composer** | `app-composer-base`, `-menus`, `-pills`, `-attach`, `-readout`, `-palette`, `-boot`, `-auth` + `app-composer.js` | Everything below the transcript, plus the boot and sign-in screens. |
| **permissions** | `app-permissions.js` | Permission, question and elicitation cards. |
| **dashboard** | `app-session-base`, `-cards`, `-mcp`, `-workloads`, `-git` + `app-session.js` | The session dashboard and its views. |
| **tabs** | `app-tabs-base`, `-guard`, `-tree`, `-pill`, `-scroll` + `app-tabs.js` | The chat and agent tab bar. |

Plus `shell.html` (the CSP'd document), `css/` (concatenated into a single `<style>` block in the cascade
order declared by `JcefHost.CSS_PARTS`, which is where that list lives), and the
vendored `marked` / `highlight.js` / `DOMPurify`.

<!-- MAP:GENERATED BEGIN -->
<!-- Generated by scripts/gen-projectmap.py. Everything between these markers is overwritten on the
     next run; the prose outside them is not. `./gradlew checkProjectMap` fails when they disagree. -->

## Load order — `JcefHost.appNames`, and it is a contract

There is no module system in the page: each file is its own hash-pinned `<script>` and they meet
through `window.cc` / `window.CC`, so a file that reads another's namespace at load time must come
after it. Changing this order means changing `JcefHost.appNames`, which is where it is decided.

1. `app-core.js`
2. `app-core-markdown.js`
3. `app-core-diagram.js`
4. `app-core-theme.js`
5. `app-transcript.js`
6. `app-transcript-rows.js`
7. `app-transcript-tools.js`
8. `app-transcript-links.js`
9. `app-transcript-find.js`
10. `app-composer-base.js`
11. `app-composer-menus.js`
12. `app-composer-pills.js`
13. `app-composer-attach.js`
14. `app-composer-readout.js`
15. `app-composer-palette.js`
16. `app-composer-boot.js`
17. `app-composer-auth.js`
18. `app-composer.js`
19. `app-permissions.js`
20. `app-session-base.js`
21. `app-session-cards.js`
22. `app-session-mcp.js`
23. `app-session-workloads.js`
24. `app-session-git.js`
25. `app-session.js`
26. `app-tabs-base.js`
27. `app-tabs-guard.js`
28. `app-tabs-tree.js`
29. `app-tabs-pill.js`
30. `app-tabs-scroll.js`
31. `app-tabs.js`

## Modules — one subject each

| Module | Owns |
|---|---|
| `app-core.js` | Claude Code JCEF shell core. |
| `app-core-markdown.js` | Markdown rendering and code-block chrome. |
| `app-core-diagram.js` | the node diagram (Workloads, and the subtab popup). |
| `app-core-theme.js` | the IDE theme, reduced motion, Vibe Mode and the one-shot render diagnostics. |
| `app-transcript.js` | the transcript itself: the row registry and the pass that places each row. |
| `app-transcript-rows.js` | the per-speaker row builders. |
| `app-transcript-tools.js` | the tool card. |
| `app-transcript-links.js` | jump-to-code in model text. |
| `app-transcript-find.js` | in-transcript search and the find bar. |
| `app-composer-base.js` | the composer family's shared plumbing. |
| `app-composer-menus.js` | the floating popup menus. |
| `app-composer-pills.js` | the provider / model / mode / effort / thinking chips. |
| `app-composer-attach.js` | attachments: the 📎 menu, the chip row, and images. |
| `app-composer-readout.js` | the session line above the prompt box. |
| `app-composer-palette.js` | the slash-command palette. |
| `app-composer-boot.js` | the boot screen and the "Claude Code was not found" card. |
| `app-composer-auth.js` | the sign-in card. |
| `app-composer.js` | cc.state(s), cc.meta(m), cc.openPalette(), cc.focusInput() |
| `app-permissions.js` | the permission cards. |
| `app-session-base.js` | the dashboard family's shared plumbing. |
| `app-session-cards.js` | the Session view's own cards. |
| `app-session-mcp.js` | the MCP servers card. |
| `app-session-workloads.js` | the Workloads diagram. |
| `app-session-git.js` | the Git view. |
| `app-session.js` | the dashboard panel, and which of its views you are looking at. |
| `app-tabs-base.js` | the tab bar's shared state and lookups. |
| `app-tabs-guard.js` | the flicker guard, whole. |
| `app-tabs-tree.js` | the panel behind the ⋮: every agent, subagent and background task, as a menu. |
| `app-tabs-pill.js` | one tab, and the ⋮ that hangs the tree off it. |
| `app-tabs-scroll.js` | how the row of chats MOVES. |
| `app-tabs.js` | with the whole tree of subtabs a hover away. |

## Registrations — the page's public surface

`cc.*` is what Kotlin calls. `CC.*` is what the modules share. Listed at the line that creates it.

| Registration | Where |
|---|---|
| `CC.send` | `app-core.js:24` |
| `CC.escape` | `app-core.js:38` |
| `CC.h` | `app-core.js:53` |
| `CC.resetInShort` | `app-core.js:143` |
| `CC.resetIn` | `app-core.js:151` |
| `CC.on` | `app-core.js:161` |
| `CC.emit` | `app-core.js:171` |
| `CC.els` | `app-core.js:192` |
| `CC.announce` | `app-core.js:216` |
| `CC.flashCopied` | `app-core.js:295` |
| `CC.markdown` | `app-core-markdown.js:18` |
| `CC.decorateOneCodeBlock` | `app-core-markdown.js:125` |
| `CC.languageForPath` | `app-core-markdown.js:191` |
| `CC.diagramLabel` | `app-core-diagram.js:71` |
| `CC.diagram` | `app-core-diagram.js:78` |
| `CC.panView` | `app-core-diagram.js:326` |
| `CC.applyTheme` | `app-core-theme.js:29` |
| `CC.reducedMotion` | `app-core-theme.js:64` |
| `CC.diagnostics` | `app-core-theme.js:72` |
| `cc.diagnostics` | `app-core-theme.js:102` |
| `CC.vibeOn` | `app-core-theme.js:168` |
| `CC.isVibe` | `app-core-theme.js:170` |
| `CC.nyanSvg` | `app-core-theme.js:175` |
| `cc.theme` | `app-core-theme.js:198` |
| `CC.transcript.el` | `app-transcript.js:112` |
| `CC.transcript.safeSend` | `app-transcript.js:113` |
| `CC.transcript.conversationEl` | `app-transcript.js:114` |
| `CC.transcript.rows` | `app-transcript.js:115` |
| `CC.transcript.toolCards` | `app-transcript.js:116` |
| `cc.batch` | `app-transcript.js:379` |
| `cc.clear` | `app-transcript.js:420` |
| `cc.trimRows` | `app-transcript.js:508` |
| `CC.transcript.builderFor` | `app-transcript-rows.js:130` |
| `cc.toggleReasoning` | `app-transcript-rows.js:169` |
| `CC.transcript.toolIconSvg` | `app-transcript-tools.js:39` |
| `CC.transcript.buildTool` | `app-transcript-tools.js:49` |
| `CC.transcript.applyToolElapsed` | `app-transcript-tools.js:127` |
| `CC.transcript.applyToolState` | `app-transcript-tools.js:143` |
| `CC.transcript.routeToolOutput` | `app-transcript-tools.js:167` |
| `CC.transcript.renderCommandBlock` | `app-transcript-tools.js:288` |
| `CC.transcript.jbHref` | `app-transcript-tools.js:309` |
| `CC.transcript.renderToolLabel` | `app-transcript-tools.js:320` |
| `CC.transcript.requestLinks` | `app-transcript-links.js:86` |
| `cc.links` | `app-transcript-links.js:193` |
| `CC.transcript.runSearch` | `app-transcript-find.js:150` |
| `CC.transcript.refreshSearch` | `app-transcript-find.js:153` |
| `CC.transcript.resetSearch` | `app-transcript-find.js:160` |
| `CC.transcript.updateFindCount` | `app-transcript-find.js:197` |
| `CC.composer.h` | `app-composer-base.js:54` |
| `CC.composer.send` | `app-composer-base.js:55` |
| `CC.composer.els` | `app-composer-base.js:59` |
| `CC.composer.lastState` | `app-composer-base.js:61` |
| `CC.composer.hostClipboard` | `app-composer-base.js:63` |
| `CC.composer.openMenu` | `app-composer-menus.js:18` |
| `CC.composer.menuSig` | `app-composer-menus.js:41` |
| `CC.composer.togglePillMenu` | `app-composer-menus.js:43` |
| `CC.composer.positionMenu` | `app-composer-menus.js:144` |
| `CC.composer.closeMenu` | `app-composer-menus.js:159` |
| `CC.composer.PILL_DEFS` | `app-composer-pills.js:58` |
| `CC.composer.buildPill` | `app-composer-pills.js:94` |
| `CC.composer.renderPills` | `app-composer-pills.js:127` |
| `CC.composer.syncOpenMenu` | `app-composer-pills.js:155` |
| `CC.composer.attachGlyph` | `app-composer-attach.js:20` |
| `CC.composer.toggleAttachMenu` | `app-composer-attach.js:199` |
| `cc.attachData` | `app-composer-attach.js:215` |
| `CC.composer.renderAttachments` | `app-composer-attach.js:266` |
| `CC.composer.wireImageDrop` | `app-composer-attach.js:299` |
| `CC.composer.insertAtCursor` | `app-composer-attach.js:329` |
| `CC.composer.wireImagePaste` | `app-composer-attach.js:344` |
| `cc.attachments` | `app-composer-attach.js:402` |
| `CC.composer.renderReadout` | `app-composer-readout.js:16` |
| `CC.composer.openPalette` | `app-composer-palette.js:151` |
| `CC.composer.setCommands` | `app-composer-palette.js:354` |
| `cc.openPalette` | `app-composer-palette.js:360` |
| `CC.composer.renderBoot` | `app-composer-boot.js:27` |
| `CC.composer.setInstallMethods` | `app-composer-boot.js:224` |
| `cc.bootPathError` | `app-composer-boot.js:232` |
| `CC.composer.authForced` | `app-composer-auth.js:17` |
| `cc.authState` | `app-composer-auth.js:63` |
| `cc.showAuth` | `app-composer-auth.js:69` |
| `CC.composer.authWanted` | `app-composer-auth.js:91` |
| `CC.composer.renderAuth` | `app-composer-auth.js:115` |
| `CC.composer.ensureBuilt` | `app-composer.js:206` |
| `CC.composer.autosize` | `app-composer.js:266` |
| `cc.state` | `app-composer.js:414` |
| `cc.meta` | `app-composer.js:425` |
| `cc.focusInput` | `app-composer.js:431` |
| `cc.insertText` | `app-composer.js:442` |
| `CC.dash.core` | `app-session-base.js:101` |
| `CC.dash.conversation` | `app-session-base.js:102` |
| `CC.dash.appRoot` | `app-session-base.js:103` |
| `CC.dash.h` | `app-session-base.js:104` |
| `CC.dash.send` | `app-session-base.js:105` |
| `CC.dash.num` | `app-session-base.js:106` |
| `CC.dash.fmtInt` | `app-session-base.js:107` |
| `CC.dash.fmtUsd` | `app-session-base.js:108` |
| `CC.dash.statRow` | `app-session-base.js:109` |
| `CC.dash.card` | `app-session-base.js:110` |
| `CC.dash.leaveDashboard` | `app-session-base.js:119` |
| `CC.dash.buildPlanCard` | `app-session-cards.js:258` |
| `CC.dash.buildUsageCard` | `app-session-cards.js:259` |
| `CC.dash.buildContextCard` | `app-session-cards.js:260` |
| `CC.dash.buildCostCard` | `app-session-cards.js:261` |
| `CC.dash.buildAccountCard` | `app-session-cards.js:262` |
| `CC.dash.buildEnvCard` | `app-session-cards.js:263` |
| `CC.dash.buildMcpCard` | `app-session-mcp.js:123` |
| `CC.dash.buildWorkloadsCard` | `app-session-workloads.js:225` |
| `CC.dash.buildGitHeadCard` | `app-session-git.js:311` |
| `CC.dash.buildGitActionsCard` | `app-session-git.js:312` |
| `CC.dash.buildGitHistoryCard` | `app-session-git.js:313` |
| `CC.dash.toggleDashboard` | `app-session.js:414` |
| `CC.dash.dashboardShown` | `app-session.js:415` |
| `cc.session` | `app-session.js:428` |
| `cc.mcp` | `app-session.js:434` |
| `cc.openDashboard` | `app-session.js:456` |
| `cc.closeDashboard` | `app-session.js:470` |
| `CC.tabbar.state` | `app-tabs-base.js:27` |
| `CC.tabbar.selected` | `app-tabs-base.js:30` |
| `CC.tabbar.openPanel` | `app-tabs-base.js:33` |
| `CC.tabbar.send` | `app-tabs-base.js:90` |
| `CC.tabbar.bar` | `app-tabs-base.js:91` |
| `CC.tabbar.nodeById` | `app-tabs-base.js:92` |
| `CC.tabbar.taskById` | `app-tabs-base.js:93` |
| `CC.tabbar.pruneSelection` | `app-tabs-base.js:94` |
| `CC.tabbar.isSelected` | `app-tabs-base.js:95` |
| `CC.tabbar.depthOf` | `app-tabs-base.js:96` |
| `CC.tabbar.hasWork` | `app-tabs-base.js:97` |
| `CC.tabbar.drawn` | `app-tabs-guard.js:88` |
| `CC.tabbar.drawnSignature` | `app-tabs-guard.js:90` |
| `CC.tabbar.openTree` | `app-tabs-tree.js:360` |
| `CC.tabbar.closeTree` | `app-tabs-tree.js:361` |
| `CC.tabbar.scheduleOpen` | `app-tabs-tree.js:362` |
| `CC.tabbar.scheduleClose` | `app-tabs-tree.js:363` |
| `CC.tabbar.pill` | `app-tabs-pill.js:141` |
| `CC.tabbar.scrollLeftTo` | `app-tabs-scroll.js:127` |
| `CC.tabbar.dragToScroll` | `app-tabs-scroll.js:128` |
| `CC.tabbar.wheelToScroll` | `app-tabs-scroll.js:129` |
| `CC.tabbar.keepFocusVisible` | `app-tabs-scroll.js:130` |
| `CC.tabbar.showChat` | `app-tabs.js:60` |
| `CC.tabbar.showAgent` | `app-tabs.js:61` |
| `CC.tabbar.showTask` | `app-tabs.js:62` |

## Cascade order — `JcefHost.CSS_PARTS`, and it is a contract too

`css/` is one stylesheet, split into parts and concatenated into a single `<style>` block in
this order. Order decides which rule wins at equal specificity, so it is a contract exactly
like the load order above, and it is changed in the same place: `JcefHost.CSS_PARTS`.

1. `css/base.css`
2. `css/transcript.css`
3. `css/composer.css`
4. `css/permissions.css`
5. `css/dashboard.css`
6. `css/git.css`
7. `css/boot.css`
8. `css/tabs.css`

<!-- MAP:GENERATED END -->

## Conventions here

- **Load order is a contract**, and it is declared in `JcefHost.appNames`, not by the directory listing. A
  file that reads another's namespace at load time must come after it: `app-core.js` first, then each
  family's shared namespace before its members, and the composer's, the dashboard's and the tab bar's spines
  last, because those build eagerly on load.
- **CSS cascade order is the same kind of contract**, declared in `JcefHost.CSS_PARTS`. The parts are
  concatenated in that order into one block; alphabetical is not the same order and would silently change
  which rule wins.
- **A layer goes in the work area — and it is `#work`'s `z-index: 0` that makes that mean anything.** A new
  floating surface is a sibling of `#conversation`, inside `#work`; never a `position: fixed` panel over
  `#app`. Inside that box the roles are settled and ordered — transcript, then a layer over it, then the
  dock, then the palette, then the find bar — each declared at its own rule and argued there. `#tabsbar` is
  outside `#work` and the dock is a row of its own, so nothing laid over the conversation can reach the tabs
  or the composer. **What holds all of that up is none of the five numbers: it is the `z-index: 0` on the
  positioned `#work`**, which is what opens the stacking context that scopes them. Take it away and they go
  back to competing at the root — where the body-mounted popups live, and where the three-digit values came
  from — and the containment comes apart **silently**: nothing looks wrong until somebody adds the next
  layer. That is why the contract asserts the stacking context *first* and the ordering only after, and why
  it asserts the ordering as relations between the roles rather than as digits.
- **The popups are mounted on `document.body` deliberately, and "it floats" is not the reason.** A menu
  hanging off a tab has to escape `#tabsbar`, which clips its overflow — that is the whole criterion. The
  find bar floats too and belongs *inside* `#work`: mounted on the body it was positioned against the
  viewport, and in a narrow tool window it covered a focused chat tab outright, which is WCAG 2.2 SC 2.4.11
  (Focus Not Obscured). Escaping a clip is a reason to leave the work area. Nothing else is.
- **A module header's `Owns:` line is the row this map prints for it**, and it outranks the em-dash subject
  on the title line. That is the sentence to edit when a module's row reads wrong — not the table, which is
  generated.
- **Nothing is fetched.** Every dependency is vendored, every script and style is hash-pinned in the CSP.
  Adding a library means adding bytes to the artifact and a hash to the policy — there is no third option.
- **Links never navigate.** They route through the host, which decides whether the destination is openable.
- Model text is rendered through `marked` and then sanitised through `DOMPurify`. Never inject model text
  another way.
- A handler on an element that gets re-serialised through `innerHTML` **is lost**. Delegate from `document`.
- **The page paints the status word it is sent**; it does not derive one.
- Accessibility is part of the contract, not a later pass: the live region is declared in the static shell so
  the first write is announced, every suppressed outline has a `:focus-visible` replacement, and there is a
  `forced-colors` fallback.

## Minefields here

- **A guard that skips a render must cover everything drawn**, including an open panel — otherwise a pointer
  resting on a tab means a full rebuild, a re-anchor and a reopen under the cursor several times a second.
  That was the reported flicker, and the guard's stamps only make sense together, which is why they stay in
  one file.
- **Do not "keep the DOM fresh while hidden."** The dashboard did exactly that and laid out a diagram nobody
  was looking at. Stash the payload; render on show.
- **Chromium does not move a horizontal scroller with a vertical wheel.** The gesture has to be translated,
  and focus has to be scrolled into view, or tabbing leaves focus off-screen.
- **Mouse events, not pointer events, deliberately**: `setPointerCapture` does not exist in jsdom, and this
  broke twice with no test noticing.
- **A full-window overlay covers whatever is laid under it.** The waiting screens are children of the
  transcript, not absolutely-positioned panels over the app, because a row cannot cover what it does not own.
- **A control that overlaps a focusable element is a WCAG 2.2 failure** (Focus Not Obscured). Put it in the
  flow instead of keeping a padding in sync with the width of the words.
- **A bounded row needs both a bound and a cap**: without a width bound the row grows and its overflow is
  painted outside the tool window, where nothing can scroll to it; without a `max-width` on the label,
  `text-overflow` never triggers.
- **A delay is not a duration, and a waiting screen needs the delay.** A screen that must not appear on a fast
  start is held back by `animation-delay` plus `both` — `both` is what applies the 0% keyframe before the
  animation begins, so the element sits at opacity 0 through the grace. Lengthening the *duration* instead
  still paints frame one, which is the flash it was supposed to prevent.
- **`prefers-reduced-motion` must zero the delay too, not just the duration.** Zeroing only
  `animation-duration` leaves a timed reveal running under the preference: the element waits out its delay
  invisibly and then snaps in. The reset in `boot.css` sets `animation-delay` and `transition-delay` to `0s`
  as well, and being `!important` on a universal selector it reaches every part, including `dashboard.css`.
- **Stagger by CLASS, never by `:nth-child()`**, on anything whose siblings are toggled with `hidden`. Every
  `.auth-step` container exists at once with all but one hidden, so position counting hands the visible step
  the beat of its index in a list where exactly one is ever displayed.
- **`animation-delay` goes after the `animation` shorthand, always.** The shorthand resets it, so the reverse
  order silently drops the delay.
- Never write a round, an agent name or a development note into a module header. The header states what the
  module owns, and it is read verbatim into the generated map.

## Neighbours

- The payloads and the browser host → [`../../kotlin/dev/lain/claudejb/ui/jcef/`](../../kotlin/dev/lain/claudejb/ui/jcef/PROJECTMAP.md)
- The assembler that pushes them → [`../../kotlin/dev/lain/claudejb/ui/`](../../kotlin/dev/lain/claudejb/ui/PROJECTMAP.md)
- The tests that drive this surface → [`../../../test/frontend/`](../../../test/frontend/PROJECTMAP.md)
