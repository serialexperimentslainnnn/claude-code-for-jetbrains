package sample

/** A sample file the UI tests can reference for @-mention, jump-to-code and diff scenarios. */
fun greet(name: String): String = "Hello, $name"

fun main() {
    println(greet("Claude"))
}
